# HealthOps Studio — Backend Dependencies
# Install: pip install -r requirements.txt

# ── Core Framework ────────────────────────────────────────
fastapi==0.115.0
uvicorn==0.30.6
pydantic==2.9.0
pydantic-settings==2.5.0     # Env-based config (BaseSettings)

# ── Database ──────────────────────────────────────────────
sqlalchemy==2.0.35
psycopg2-binary==2.9.9       # PostgreSQL driver
alembic==1.13.2               # Schema migrations

# ── Auth ──────────────────────────────────────────────────
python-jose[cryptography]==3.3.0   # JWT (RS256 support via cryptography backend)
python-multipart==0.0.9            # Form data parsing (required by OAuth2PasswordBearer)
passlib==1.7.4                     # Password hashing utilities
bcrypt==4.1.3                      # Bcrypt backend
argon2-cffi
# --- Workflow Execution ---
celery[redis]==5.3.6

# --- AI Integration ---
google-genai==0.5.0

# ── Redis ─────────────────────────────────────────────────
redis==5.1.0                  # Rate limiting, token blacklist, Celery broker

# ── Utilities ─────────────────────────────────────────────
python-dotenv==1.0.1          # .env file loading (fallback for non-pydantic-settings usage)
Jinja2==3.1.4                 # Templating engine for code generation
